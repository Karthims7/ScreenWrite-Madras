# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration (SQLite is file-based)
# Database file will be created in ./data/screenplays.db

# Optional: Custom database path (relative to data directory)
# DATABASE_PATH=./data/screenplays.db
